name: Deploy to Hostinger

on:
  push:
    branches: [main]
    paths:
      - 'index.html'
      - '04_Design_Assets/Production_Photos/**'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.4.0
        with:
          server: 82.29.199.8
          username: u657502449
          password: ${{ secrets.FTP_PASSWORD }}
          port: 21
          protocol: ftps
          server-dir: /home/u657502449/domains/toolsandtable.com/public_html/cookbook
          local-dir: ./
          exclude: |
            **/.git*
            **/.git*/**
            **/.github/**
            **/.claude/**
            **/node_modules/**
            **/temporary screenshots/**
            **/*.md
            **/*.mjs
            **/*.py
            **/*.txt
            **/package.json
            **/package-lock.json
            01_Intelligence/**
            02_Hardware_Specs/**
            03_SOP_Recipes/**
            03_SOP_Recipes_V2/**
            05_Logistics_Supply/**
            06_Marketing_Comms/**
            CNAME
